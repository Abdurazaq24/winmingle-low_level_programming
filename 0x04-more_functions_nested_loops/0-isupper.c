#include <stdio.h>

/* Write a function that checks for uppercase character.
*  Learning c is fun
*/

 int _isupper(int c)
{ 

	if (c >= 'A' && c <= 'z')
	{
		return (1);
	}

	else
	{
		return(0);
	}

} 
